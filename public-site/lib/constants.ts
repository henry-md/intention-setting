// Whether to require sign in to view public stats from someone else's share link
export const REQUIRE_SIGN_IN_TO_SPY = false;

// Default daily usage reset boundary in local time (HH:mm)
export const DEFAULT_DAILY_RESET_TIME = '04:00';
